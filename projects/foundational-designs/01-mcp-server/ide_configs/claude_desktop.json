{
  "_comment": [
    "Claude Desktop MCP server configuration.",
    "Config file location on macOS:",
    "  ~/Library/Application Support/Claude/claude_desktop_config.json",
    "Config file location on Windows:",
    "  %APPDATA%\\Claude\\claude_desktop_config.json",
    "Pick one of the two options below (Docker or uv) and remove the other.",
    "See: https://modelcontextprotocol.io/quickstart/user"
  ],
  "mcpServers": {
    "acme-platform": {
      "_comment": "Option A: Docker (no Python required)",
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "RAILS_API_TOKEN", "acme-mcp:latest"],
      "env": {
        "RAILS_API_TOKEN": "your-token-here"
      }
    },
    "acme-platform-uv": {
      "_comment": "Option B: uv (requires Python 3.11+ and uv installed)",
      "command": "uvx",
      "args": ["acme-mcp-server"],
      "env": {
        "RAILS_API_TOKEN": "your-token-here"
      }
    }
  }
}
